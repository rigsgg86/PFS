import Image from "next/image";import type { Vehicle } from "@/data/fleet";
export function VehicleCard({vehicle}:{vehicle:Vehicle}){return <article className="card overflow-hidden rounded-3xl"><div className="relative h-56"><Image src={vehicle.image} alt={`${vehicle.year} ${vehicle.make} ${vehicle.model}`} fill className="object-cover"/></div><div className="p-5"><div className="flex items-center justify-between gap-3"><h2 className="text-xl font-bold">{vehicle.year} {vehicle.make} {vehicle.model}</h2><span className="rounded-full bg-gold px-3 py-1 text-xs font-bold text-ink">{vehicle.status}</span></div><p className="mt-1 text-sm text-white/60">{vehicle.trim} • {vehicle.plate}</p><dl className="mt-5 grid grid-cols-2 gap-3 text-sm"><div><dt className="text-white/45">Mileage</dt><dd>{vehicle.mileage.toLocaleString()}</dd></div><div><dt className="text-white/45">Assigned</dt><dd>{vehicle.assignedTo}</dd></div><div><dt className="text-white/45">Docs</dt><dd>{vehicle.documents}</dd></div><div><dt className="text-white/45">Photos</dt><dd>{vehicle.photos}</dd></div></dl></div></article>}
