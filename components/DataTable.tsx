interface DataTableProps<T> { title: string; description: string; columns: string[]; rows: T[]; renderRow: (row: T) => string[]; id: string; }

export function DataTable<T extends { id: string }>({ title, description, columns, rows, renderRow, id }: Readonly<DataTableProps<T>>) {
  return <section id={id} className="rounded-3xl border border-fleet-gold/20 bg-fleet-charcoal/80 p-5 shadow-gold"><div className="mb-5 flex flex-col gap-1 md:flex-row md:items-end md:justify-between"><div><h2 className="text-2xl font-black text-white">{title}</h2><p className="text-sm text-fleet-cream/60">{description}</p></div><span className="text-sm font-bold text-fleet-gold">{rows.length} records</span></div><div className="overflow-x-auto"><table className="w-full min-w-[720px] border-separate border-spacing-y-2 text-left text-sm"><thead><tr>{columns.map((column) => <th className="px-4 py-2 text-xs uppercase tracking-[0.25em] text-fleet-gold/80" key={column}>{column}</th>)}</tr></thead><tbody>{rows.map((row) => <tr className="rounded-2xl bg-white/[0.04]" key={row.id}>{renderRow(row).map((cell, index) => <td className="px-4 py-4 text-fleet-cream/85 first:rounded-l-2xl last:rounded-r-2xl" key={`${row.id}-${index}`}>{cell}</td>)}</tr>)}</tbody></table></div></section>;
}
