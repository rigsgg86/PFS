"use client";
import { signIn } from "next-auth/react";import { useState } from "react";
export default function Login(){const[error,setError]=useState("");return <main className="grid min-h-screen place-items-center bg-[radial-gradient(circle_at_top,#44340f,#050505_55%)] p-6"><form className="card w-full max-w-md rounded-3xl p-8" onSubmit={async e=>{e.preventDefault();const f=new FormData(e.currentTarget);const res=await signIn("credentials",{email:f.get("email"),password:f.get("password"),callbackUrl:"/dashboard",redirect:false});if(res?.ok) location.href=res.url ?? "/dashboard"; else setError("Invalid administrator credentials.")}}><h1 className="text-3xl font-bold text-gold">Admin Login</h1><p className="mt-2 text-white/60">Secure access for fleet administrators.</p><label className="mt-8 block text-sm">Email<input className="mt-2 w-full rounded-xl border border-gold/20 bg-black p-3" name="email" type="email" required/></label><label className="mt-4 block text-sm">Password<input className="mt-2 w-full rounded-xl border border-gold/20 bg-black p-3" name="password" type="password" required/></label>{error&&<p className="mt-4 text-sm text-red-300">{error}</p>}<button className="mt-6 w-full rounded-xl bg-gold p-3 font-bold text-ink">Sign in</button></form></main>}
