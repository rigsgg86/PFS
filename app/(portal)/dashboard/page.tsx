import { StatCard } from "@/components/stat-card";import { vehicles,maintenance,fuelLogs } from "@/data/fleet";
export default function Dashboard(){return <><h1 className="text-4xl font-bold">Dashboard</h1><p className="mt-2 text-white/60">Real-time command center for your premium fleet.</p><div className="mt-8 grid gap-4 md:grid-cols-4"><StatCard label="Vehicles" value={`${vehicles.length}`} detail="Seeded Mercedes-Benz Sprinters"/><StatCard label="Active" value={`${vehicles.filter(v=>v.status==='Active').length}`} detail="Ready for dispatch"/><StatCard label="Open service" value={`${maintenance.filter(m=>m.status!=='Complete').length}`} detail="Scheduled or in progress"/><StatCard label="Fuel spend" value={`$${fuelLogs.reduce((s,f)=>s+f.cost,0).toFixed(0)}`} detail="Recent logs"/></div><section className="card mt-8 rounded-3xl p-6"><h2 className="text-2xl font-semibold text-gold">Service reminders</h2><div className="mt-4 grid gap-3">{vehicles.map(v=><div key={v.id} className="rounded-2xl bg-black/35 p-4"><b>{v.year} {v.make} {v.model} {v.trim}</b><p className="text-sm text-white/60">Next service at {v.nextServiceMiles.toLocaleString()} miles or {v.nextServiceDate}</p></div>)}</div></section></>}
