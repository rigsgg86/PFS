import { maintenance } from "@/data/fleet";
export default function Page(){const rows=maintenance;return <><h1 className="text-4xl font-bold capitalize">maintenance</h1><p className="mt-2 text-white/60">Searchable operational records for Prime Fleet Portal.</p><div className="card mt-8 overflow-x-auto rounded-3xl p-4"><table className="w-full min-w-[620px] text-left text-sm"><thead className="text-gold"><tr>{Object.keys(rows[0]).map(k=><th className="p-3 capitalize" key={k}>{k}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr className="border-t border-white/10" key={i}>{Object.values(r).map((v,j)=><td className="p-3" key={j}>{String(v)}</td>)}</tr>)}</tbody></table></div></>}
